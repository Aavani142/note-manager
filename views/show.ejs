<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= filename.replace('.txt', '') %> | Note Viewer</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white font-sans min-h-screen">

  <div class="max-w-4xl mx-auto px-4 py-10">
    
    <!-- Back Link -->
    <div class="mb-6">
      <a href="/" class="text-blue-400 hover:underline text-sm">&larr; Back to Home</a>
    </div>

    <!-- File Viewer Card -->
    <div class="bg-zinc-800 p-8 rounded-xl shadow-lg border border-zinc-700">
      <h1 class="text-3xl font-bold text-blue-400 mb-4 border-b border-zinc-600 pb-2"><%= filename.replace('.txt', '') %></h1>
      <p class="text-base leading-relaxed whitespace-pre-wrap text-zinc-300 tracking-wide"><%= filedata %></p>
    </div>

  </div>

</body>
</html>

